This folder mostly corresponds with the contents of
@/types/api, and @/lib/api – they're simply different 
levels around the underlying API route.

This folder will contain React Query hooks, which will rely
on the lib folder, which, in turn, relies on the types folder.