export const metadata = {
    title: "Login - Bay",
    description: "Login to your Bay account",
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>
}